{
  "name": "selecta",
  "version": "0.1.0",
  "private": true,
  "description": "Local MCP server exposing the Apple Music library to Claude.",
  "type": "module",
  "bin": {
    "selecta": "dist/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && vitest run --tags-filter='!integration'",
    "test:integration": "vitest run --tags-filter=integration",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "verify:echo": "npm run build && node scripts/verify-echo.mjs",
    "dev": "node dist/index.js",
    "lint": "oxlint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "npm test && npm run lint && npm run format:check"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "better-sqlite3": "^13.0.3",
    "commander": "^15.0.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^26.4.0",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
